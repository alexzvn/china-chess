Start work to resolve issue and commit one by one when it's pass the test.
Using tool like vue-tsc, playwright mcp, ... to verify lint and actual UI test.
Using `bun test` to verify for backend and `tsc` for style
Each issue need to verify with playwright if needed to find and fix before commit.