# 17 — Documentation

> *This was generated by AI during triage.*

**Status:** needs-triage | enhancement
**Labels:** needs-triage, enhancement

## Parent

`.scratch/chinese-chess-online-game/issue.md`

## What to build

A comprehensive README.md at the project root covering: project description, tech stack, architecture overview with diagram (text-based), setup instructions (`bun install`, how to run dev, how to build for production, how to run tests), WebSocket protocol specification (all message types, payloads, and flows), Chinese Chess rules reference (piece movements, check/checkmate/stalemate), component breakdown (what each Vue component does), and the iterative development cycle description. Final `.gitignore` excludes `node_modules/`, `.env*`, `*.log`, `coverage/`, `.turbo/`, `*.tsbuildinfo`.

## Acceptance criteria

- [ ] README includes: project description, tech stack, setup instructions
- [ ] Setup instructions: `bun install`, two-terminal dev, production build, `bun test`
- [ ] WebSocket protocol section: table of all message types (Client→Server, Server→Client) with JSON payload examples
- [ ] Chinese Chess rules section: piece list, movement rules summary, check/checkmate/stalemate definitions
- [ ] Architecture section: text-based diagram showing Bun → Elysia → WS/HTTP → Vue frontend flow
- [ ] Component breakdown: description of each Vue component's responsibility
- [ ] `.gitignore` contains all necessary exclusions
- [ ] Links to `docs/adr/` and `CONTEXT.md` for deeper reading

## Blocked by

- 01-project-scaffold
