# 10 — Turn Indicator + Check Visual

> *This was generated by AI during triage.*

**Status:** needs-triage | enhancement
**Labels:** needs-triage, enhancement

## Parent

`.scratch/chinese-chess-online-game/issue.md`

## What to build

The active player's area (PlayerInfo.vue section) gets a visual highlight (glow/border) to indicate whose turn it is. No text label ("Red to move") — purely visual. When a king is in check, the king piece on the board gets a red glow/border. The turn indicator updates after each `boardUpdate` from the server. The check glow appears on the king when the engine reports the position is check.

## Acceptance criteria

- [ ] Active player's `PlayerInfo.vue` area has a visible highlight (e.g., brighter border, subtle glow) when it's their turn
- [ ] Inactive player's area has a muted/dimmed appearance
- [ ] Highlight switches sides on every `boardUpdate` (turn swap)
- [ ] When the current turn's king is in check, the king piece on the board has a red glow/border (CSS `box-shadow` or Tailwind ring)
- [ ] Check glow disappears when the check is resolved (test: move a piece to block or capture the checking piece)
- [ ] Check is computed server-side via the engine and included as `inCheck: true|false` in `boardUpdate`
- [ ] Component test: verify `PlayerInfo` receives and renders `activePlayer: "red"|"black"` prop correctly

## Blocked by

- 09-move-execution-e2e
