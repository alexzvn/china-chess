# 26 — Refactor WebSocket handler with dispatch table

> *This was generated by AI during triage.*

**Status:** needs-triage | enhancement
**Labels:** needs-triage, enhancement

## What to build

Replace the ~450-line inline WebSocket message handler in `src/server/index.ts` with a ~35-line dispatch table that routes actions to adapters from `src/server/actions/`.

**Handler becomes:**
1. Handle `ping`/`pong` (protocol-level, stays inline)
2. Build `ActionContext` from message data
3. Route action name to `allActions[action](ctx)`
4. Process `ActionResult`: send errors to client, deliver notifications

**What gets deleted:**
- All inline `if (action === "...")` blocks (~400 lines)
- `broadcastRoomUpdate` function (notifications now go through the adapter pattern)
- Ad-hoc `Record<string, unknown>` message shapes (replaced by `ServerMessage` type)

**What stays inline:**
- `ping`/`pong` (protocol-level heartbeat)
- `reclaimRoom` (connection-layer mutation of `clientConnections` map)

**Tests:**
- Update `index.test.ts` and `ws.test.ts` to use the new dispatch pattern
- Verify all existing WebSocket flows still work (room creation, joining, moves, kicks, rematch, reconnection, chat, resign, draw)

## Acceptance criteria

- [ ] Handler in `index.ts` is ~35 lines (dispatch table + result processing)
- [ ] All 14 action adapters imported from `actions/index.ts`
- [ ] `ping`/`pong` and `reclaimRoom` stay inline
- [ ] Old inline handlers completely deleted
- [ ] `broadcastRoomUpdate` function removed
- [ ] All existing tests pass (`bun test`)
- [ ] End-to-end: room creation, join, toggleReady, move, check/checkmate, resign, draw, kick, rematch, reconnection, chat all work

## Blocked by

- 23-room-action-adapters
- 24-game-action-adapters
- 25-phase-action-adapters
