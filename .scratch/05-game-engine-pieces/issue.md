# 05 — Game Engine: Piece Movement Rules

> *This was generated by AI during triage.*

**Status:** needs-triage | enhancement
**Labels:** needs-triage, enhancement

## Parent

`.scratch/chinese-chess-online-game/issue.md`

## What to build

A pure TypeScript module implementing all 7 Chinese Chess piece movement rules. Exports `isValidMove(pieceType, from, to, board, moveHistory?)` that returns `boolean` for each piece type. Contains functions for sliding pieces (車, 炮), L-shape with leg-block (馬), diagonal with eye-block and river restriction (象), palace-confined pieces (士, 將/帥), and soldier movement with river crossing (兵/卒). No check detection or checkmate — just raw piece rules. Full unit test coverage via `bun test`.

The module lives at `src/server/game/engine.ts` and has zero Elysia or Vue imports.

## Acceptance criteria

- [ ] `isValidMove` accepts a position `{ rank: 0-9, file: 0-8 }` and 2D array board
- [ ] 車 slides any distance orthogonally (no blocking)
- [ ] 馬 L-shape (2+1), blocked if "leg" square adjacent in first direction is occupied
- [ ] 炮 slides orthogonally; capture requires exactly one piece (screen) between source and target
- [ ] 士 one step diagonally, confined to 3×3 palace
- [ ] 象 two steps diagonally, blocked if "eye" square occupied, cannot cross river
- [ ] 將/帥 one step orthogonally, confined to palace
- [ ] 兵/卒 one step forward; after crossing river, also one step sideways; never backward
- [ ] Unit tests for each piece type covering: normal moves, blocked moves, board-boundary moves, captures
- [ ] Unit tests for edge cases: 馬 leg-block on all 4 directions, 炮 slide without screen, 象 eye-blocked + river crossing attempt

## Blocked by

- 01-project-scaffold
