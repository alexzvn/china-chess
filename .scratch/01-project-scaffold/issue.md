# 01 — Project Scaffold + "Hello World"

> *This was generated by AI during triage.*

**Status:** needs-triage | enhancement
**Labels:** needs-triage, enhancement

## Parent

`.scratch/chinese-chess-online-game/issue.md`

## What to build

Set up the full project skeleton: Bun + ElysiaJS server that starts and responds to HTTP GET `/` with a placeholder, Vue 3 + Vite + Tailwind CSS + VueUse/core frontend that compiles without errors, `bun test` passes a trivial smoke test, and all dependencies are installed via `bun install`. The two dev terminals are documented: one for `vite`, one for `bun run src/server/index.ts`.

## Acceptance criteria

- [ ] `bun install` completes without errors, adding `elysia`, `@elysiajs/static`, `@elysiajs/cors`, `vue`, `vue-router`, `tailwindcss`, `vueuse/core`, `nanoid`, and their dev deps
- [ ] `bun run src/server/index.ts` starts an Elysia server on port 3000 responding to `GET /` with `"Chinese Chess Server"` (or similar)
- [ ] `vite` in the Vue source root starts the dev server on port 5173 and renders a Vue "Hello World" page
- [ ] Tailwind CSS is configured (`tailwind.config.ts`, `@tailwind` directives in main CSS) and utility classes compile
- [ ] VueUse/core is installed and an import like `import { useWindowSize } from '@vueuse/core'` compiles
- [ ] `bun test` runs a trivial `expect(1+1).toBe(2)` smoke test and passes
- [ ] PROJECT_ROOT/CONTEXT.md references the project, `docs/adr/` contains ADRs 001–010, `.gitignore` excludes `node_modules/`, `.env*`, `*.log`, `coverage/`, `.turbo/`, `*.tsbuildinfo`

## Blocked by

None — can start immediately.
